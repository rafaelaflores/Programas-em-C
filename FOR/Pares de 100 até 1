/*Faça um programa que imprima todos os números pares de 100 até 1*/
 #include <stdio.h>
 #include <stdlib.h>
 #include <locale.h>
	int main() {
	setlocale(LC_ALL, "Portuguese");
	int x, par=0;
	
	for(x=100;x>0;x--)
	{
		par=x;
		if(par%2==0){
		
		printf("%d ",par);
	}
	}
	return 0;
}
